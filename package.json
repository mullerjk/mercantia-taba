{
  "name": "taba-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Use: cd app && npm run dev'",
    "build": "echo 'Use: cd app && npm run build'",
    "start": "echo 'Use: cd app && npm run start'",
    "dev:app": "cd app && npm run dev",
    "dev:magicui": "echo 'MagicUI development: cd magicui/apps/www && npm run dev'",
    "build:app": "cd app && npm run build"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.17.0",
    "npm": ">=8.0.0"
  }
}
